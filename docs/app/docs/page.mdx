# Welcome

Welcome to the **parsec** documentation

Created by [Oliver Kwun-Morfitt](https://olliekm.com)

## Quick Example

```python
adapter = OpenAIAdapter(
    api_key="your-key-here",
    model="gpt-4o-mini"
)

validator = JSONValidator()

engine = EnforcementEngine(
    adapter=adapter,
    validator=validator,
    max_retries=3
)

# Define schema
schema = {
    "type": "object",
    "properties": {
        "name": {"type": "string"},
        "age": {"type": "number"},
        "email": {"type": "string"}
    },
    "required": ["name", "age", "email"]
}

# Enforce
result = await engine.enforce(
    prompt="Extract person info: John is 30 years old, john@example.com",
    schema=schema
)
```

## Features

- JSON schema and Pydantic validation
- Automatic secham repair
- *Coming soon: Decorator for extending functionality*

## Next Steps

- [Installation](./installation)
- [Quick Start](./quickstart)
- [API Reference](./api)
